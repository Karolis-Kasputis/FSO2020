name: Hello World!

on:
  push:
    branches: [master]
jobs:
  hello_world_job:
    runs-on: ubuntu-18.04

    steps:
      - uses: actions/checkout@v2
      - name: Say hello
      - run: echo "Hello World!"
